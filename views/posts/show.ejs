<!DOCTYPE html>
<html lang="en">
        <!-- Head partial -->
  <%- include('../partials/head', { page: "Show" } ) %>

<body class="has-navbar-fixed-top">
          <!-- Navbar Partial -->
          <%- include('../partials/nav', { routes } ) %>
  <!--<h2><%= user.username %></h2>  -->
  <section class="post-container">
      <div class="post-display-container">
        <p><%= post.content %></p>
      </div>
      <div class="button-container">
          <button class="like">Like</button>
          <button class="comment">Add Comment</button>
          <botton class="edit">Edit</botton>
      </div>  
      <div class="comments-container">
          <textarea class="comment-input" placeholder="Leave a comment" value="<%= comment.content %>"></textarea>
          <div class="comment-display"><%= comment.content %>
          </div>
      </div>

    </section>
</body>
</html>